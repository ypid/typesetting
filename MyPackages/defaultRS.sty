\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{defaultRS}
  [2013/06/29 v1.01 Default packages and configuration by Robin Schneider]

\RequirePackage{
  cmap,
  babel,  %% \showhyphens{Trennungen} %% sprach Unterstützung
}
\RequirePackage[T1]{fontenc}  %% use TeX encoding then Type 1
%% Verbraucht Speicher im PDF (2101 Byte)

\RequirePackage{
  amsthm,
  amssymb,     %% Zusatzsymbole z.B. Square Vartriangle etc
  textcomp,    %% Symbol z. B. \textdiscount
  ziffer,
    %% Sorgt dafür das ein Komma in $ ... $ keinen Leerraum mehr erzeugt
    %% $3,14$ = $3{,}14$
    %% isonums hat noch Probleme
    %% icomma bietet auch eine Lösung, genannt in LaTeX Hacks #18
  romanbar,
  cancel,
  calc,
  verbatim,
%  comment,
%  soulpos,    %% Unterstreichungen aller Art,
    %% auch Linien mit zufälligem Zittern
%  lcd,        %% Setzt Text im Still von LCD Displays
%  keystroke,  %% Setzt Computer Tasten von Tastaturen \Esc
  multicol,    %% \begin{multicols}{2}
  paralist,    %% besseres enumerate und itemize und neue compactenum/compactitem
  eqlist,      %% Beschreibungslisten mit gleicher Einrückung
    %% Es kann auch die labeling Umgebung aus (scrextend) der KOMA-Script Klasse benutzt werden
    %% Hier muss aber die Breite manuell definiert werden: {\textbf{1990--2000}}
    %\begin{labeling}[]{\textbf{1990--2000}}
    %\item[\textbf{1994--2002}] Gymnasium Leipzig
    %\end{labeling}
    % USE THIS instead:
    %\begin{eqlist}
    %\item[1994--2002]
    %\end{eqlist}
  rotating,    %% Text drehen
  xcolor,      %% Farbdefinitionen möglich
    %% Verbraucht Speicher im PDF (211 Byte)
    %\selectcolormodel{cmyk}    %% Aus LaTeX Hacks O'Reilly #16
    %% sorgt dafür, dass alle Farbdefinitionen in der Ausgabedatei dem cmyk-Modell folgen.
    %% Sichtbarer Unterschied …
  moreverb,      %% erweiterte verbatim Umgebung
  pdfpages,      %% Einbinden von PDF Seiten aus PDF Dokument
  nicefrac,      %% Darstellung eines Bruchs im Fließtext; Aussehen zB 1/4
  gensymb,       %% celsius, micro, ohm, perthousand, degree in Mathe und Textmodus
  %
%  lastpage,      %% durch totpages ersetzt %% \pageref{LastPage}
  fink,          %% The LaTeX2e File Name Keeper \finkpath
  %
  stringstrings, %% String manipulation
    %% \wordcount fail with \section
}

\RequirePackage[autolanguage]{numprint}
%% Ordentliches Darstellen von Zahlen im Text und Mathe Modus
\npfourdigitnosep

\RequirePackage{eurosym} %% \EUR{2342,50}
\let\RawEUR\EUR\relax
\renewcommand{\EUR}[1]{\RawEUR{\numprint{#1}}}

\RequirePackage[normalem]{ulem}  %% Unterstreichungen

\RequirePackage{enumerate}    %% \begin{enumerate}[{Teil }I] oder i,a,A,1
\newenvironment{itemizewrapper}{\begin{compactitem}}{\end{compactitem}}

\RequirePackage[babel]{csquotes} %\enquote{nur} %% Anführungszeichen ,german=quotes

\clubpenalty=10000    %% Unendlich
\widowpenalty=10000
\displaywidowpenalty=10000

%\RequirePackage{linenoWorkaroundRS}
%% Erzeugt Fehler und wird nur noch temporär eingebunden

%% language definition
\RequirePackage{fnumprint}  %% fnumprint@language
\RequirePackage{IfconstructsRS}
\twoIF{\value{fnumprint@language}}{
  \input{defaultRS-ngerman.ldf}
}{
  \input{defaultRS-english.ldf}
}{
  \PackageError{docExtensionsRS}{language definition}
    {I don't know the language you have asked for}
}

\newcommand{\printlanguage}{
\iflanguage{ngerman}{Deutsch (neue Rechtschreibung)}{
  \iflanguage{english}{Englisch}{
  \iflanguage{british}{Britisches Englisch}{\textcolor{red}{\languagename}}
  }
}}

\let\pagenumberingRaw\pagenumbering\relax%
\renewcommand{\pagenumbering}[1]{%
  \pagenumberingRaw{#1}%
  \ifthenelse{\equal{#1}{Roman}}{%
    %\renewcommand*{\pagemark}{\Roman@bar{\Roman{page}}}% not added by \pageref.
    \renewcommand*{\thepage}{\Roman@bar{\Roman{page}}}%
  }{
    %*{\pagemark}{\thepage}%
  }
}


\RequirePackage{legacyRS}

\endinput

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{RequirePackageRS}
          [2019/05/27 v1.1.1 Set options are require packages before a documentclass ist set by Robin Schneider]

\RequirePackage{iftex}
% \RequirePackage{ifthen}

\RequirePackage[l2tabu,orthodox]{nag}  %% nag überprüft den Text auf veraltete
                   % %% Befehle oder solche, die man nicht in LaTeX verwenden
                   % %% soll -- l2tabu-Checker in LaTeX

\ifPDFTeX%
  \RequirePackage[ngerman=ngerman-x-latest]{hyphsubst}
  % Einbinden der neuen Trennmuster, diese korrigieren einige Fehler der alten
  % und bieten mehr Trennstellen.
\fi

% \RequirePackage{classlist}	%% \MainClassName

% \RequirePackage{iftex}
% \ifPDFTeX%
  % \RequirePackage{fix-cm}
% \fi
%% Not using Computer modern.

\PassOptionsToPackage{
  table,
  svgnames,
  % x11names,
}{xcolor}
\PassOptionsToPackage{final}{graphicx}
\PassOptionsToPackage{final}{pdfpages}

\endinput
